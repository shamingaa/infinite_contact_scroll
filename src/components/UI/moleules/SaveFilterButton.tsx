import { Button } from "react-bootstrap";

const SaveFilterButton = (): JSX.Element => {
  return (
    <div className="saveBtnPack">
      <Button id="saveBtn">Save Filters</Button>
    </div>
  );
};

export default SaveFilterButton;
