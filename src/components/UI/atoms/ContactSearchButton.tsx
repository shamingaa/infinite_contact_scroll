import { Button } from "react-bootstrap";

const ContactSearchButton = (): JSX.Element => {
  return <Button variant="outline-success searchBtn">Search</Button>;
};

export default ContactSearchButton;
